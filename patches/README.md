# patches/

Патчи, которые применяются во время сборки. Их три вида, и они применяются
на разных этапах.

| Где лежит | К чему применяется | Когда | Если не применился |
|---|---|---|---|
| `patches/*.patch` | к дереву исходников OpenWrt / ImmortalWrt (`openwrt/`) | шаг **Apply custom patches** в `Universal_WRT_Builder.yml`, до `feeds update` | сборка останавливается |
| `patches/feeds/<фид>/*.patch` | к фиду `feeds/<фид>/` | `sh/WRT-part2.sh`, между `feeds update` и `feeds install` | предупреждение в Actions, сборка идёт дальше без патча (или стоп при `FEED_PATCH_STRICT=1`) |
| `patches/packages/<пакет>/*.patch` | к пакету, который `sh/WRT-part3.sh` клонирует в `package/<пакет>` (или к нему же в фиде) | `sh/WRT-part3.sh`, сразу после клонирования | так же, как у патчей фидов |

## Патчи дерева: `patches/*.patch`

Берутся только файлы в корне каталога, подпапки не просматриваются.
Применяются по алфавиту командой

```sh
patch -p1 -d openwrt < patches/<файл>.patch
```

то есть пути в патче считаются от корня дерева OpenWrt
(`a/package/...`, `a/include/...`). Пример имени:
`001-mbedtls-fix-build-when-disabled.patch`.

На этом шаге фидов ещё нет, поэтому **патч к пакету из фида сюда класть нельзя**:
он не найдёт файлы и остановит все варианты сборки.

## Патчи фидов: `patches/feeds/<фид>/*.patch`

Имя подпапки должно совпадать с именем фида в `feeds.conf`
(первое слово после `src-git`). Пути в патче считаются от корня репозитория
фида (`a/forkop/Makefile`, `a/luci-app-forkop/...`).

`WRT-part2.sh` проходит патчи подпапки по порядку имён:

1. фида `feeds/<фид>` нет (вариант его не подключает) — пропускает всю подпапку;
2. патч уже есть в исходниках (автор внёс то же самое) — пропуск, `✓ ... уже есть`;
3. в заголовке патча есть строка `Requires: <имя патча>`, а тот патч не применён
   и не найден в исходниках — пропуск с предупреждением. Так зависимый патч
   не попадёт в код без того, на что он опирается;
4. патч применяется без сдвига контекста — применяется
   (`patch -p1 -F 0`: без «нечёткого» поиска места, чтобы при изменённом
   коде правка не встала не туда);
5. иначе — пропуск с предупреждением `::warning::` в Actions.

Каждый патч применяется целиком или не применяется вовсе. В конце выводится
итог: `>>> forkop: применено N, уже было M, пропущено K`. Чтобы сборка
останавливалась, если хоть один патч не применился, задайте переменную
окружения `FEED_PATCH_STRICT=1`.

### forkop (`patches/feeds/forkop/`, фид `forkop` → ushan0v/forkop)

Фид для варианта `forkop` выбирается в `Universal_WRT_Builder.yml` по этой папке: есть
хотя бы один `*.patch` — подключается оригинал `ushan0v/forkop` (фид `forkop`), нет —
форк `Gavr1024/forkop-mod` (фид `forkopmod`), как было до патчей. Голый оригинал 1.0.5
без патчей на sing-box 1.14 не запускается (issue #98), поэтому удалять патчи
можно без правки YML.

Оригинальный forkop 1.0.5 (коммит `dd48329`) плюс 24 патча. Номера 001–017 —
открытые PR репозитория ushan0v/forkop (авторы сохранены в заголовках),
остальные — наши исправления и доработки.

| Патч | Что делает |
|---|---|
| 001 PR83 | `hijack-dns` только по протоколу DNS, а не по порту 53 (#79) |
| 002 PR82 | нет петли FakeIP для ip.podkop.fyi при ByeDPI (#74) |
| 003 PR119 | таймаут проверки задержки в дашборде 10 с (#117) |
| 004 PR109 | Priority на монотонных часах (#96) |
| 005 PR101 | права LuCI на `/tmp/run/forkop/*` для rpcd из 25.12 |
| 006 PR104 | проверка mwan3 без падения (#115) |
| 007 PR103 | sing-box 1.14: без `independent_cache` (#98, #108) |
| 008 PR113 | не падать без IPv6 (#91) |
| 009 PR100 | загрузка компонентов: 600 с и обрыв при скорости < 1 КБ/с (#99) |
| 010 PR105 | защита от нехватки места в /tmp при загрузке sing-box (#99) |
| 011 | исправление ошибки в PR105 (`updates_log` до объявления); `Requires: 010` |
| 012 PR80 | URLTest с интервалом больше 30m (#81) |
| 013 PR90 | исключения серверов секции действуют на URLTest/Priority (#89) |
| 014 PR68 | переподключение дашборда после перезагрузки sing-box (#65) |
| 015 PR107 | правило fw4 для TPROXY из гостевой сети (#84) |
| 016 | то же правило на прошивке (через uci-defaults); `Requires: 015` |
| 017 PR106 | ByeDPI от отдельного пользователя, без повторного перехвата (#102) |
| 018 | функции ucode объявлены до вызова (#62, #87, #115) |
| 019 | `prerm` в Makefile на sh для apk (25.12) |
| 020 | sing-box 1.14: `evaluate`/`respond` и `http_client` (#98, #108); `Requires: 007` |
| 021 | таймаут загрузки списков `wget -T 30` (#127) |
| 022 | LuCI показывает домены из `list domain` (#116) |
| 023 | настройка «Исключить BitTorrent» (#72) |
| 024 | настройка «Исключённые устройства» по IP, подсети или MAC (#88); `Requires: 017, 020, 023` |
| 025 | настройка «Перехватывать DNS устройств» (`intercept_device_dns`): выключенная оставляет DNS исключённых устройств и устройств из фильтров секций резолверу перед forkop (AdGuard Home видит каждого клиента), их трафик к FakeIP forkop направляет сам; `Requires: 024` |

Проверено: применение к чистому оригиналу, к оригиналу, где автор уже принял
любую часть патчей или все открытые PR, и пропуск любого одного патча —
ни в одном случае не получается несогласованный код. Итог проходит тесты
forkop, строгую проверку ucode и проверку конфигов настоящими sing-box
1.13.21 и 1.14.2.

### forkop-mod (`patches/feeds/forkopmod/`, фид `forkopmod` → Gavr1024/forkop-mod)

Используется, только если в workflow вместо `forkop` подключён `forkopmod`.

| Патч | Что делает |
|---|---|
| 001 | функции ucode объявлены до вызова (главное: генерация конфига sing-box) и `prerm` на sh для apk |

Проверить в логе шага **Update Feeds & Config**:

```
Патчи фидов (patches/feeds/*)
✓ forkop: применён 001-pr83-hijack-dns-port53
...
>>> forkop: применено 24, уже было 0, пропущено 0
>>> Фид 'forkopmod' не загружен, патчи patches/feeds/forkopmod пропущены.
```

## Патчи пакетов: `patches/packages/<пакет>/*.patch`

Для пакетов, которых нет в фидах и которые `WRT-part3.sh` клонирует в
`package/` (сейчас это HomeProxy из `immortalwrt/homeproxy`). На этапе патчей
фидов их ещё нет, поэтому применяются они в part3, сразу после клонирования.
Имя подпапки — имя каталога пакета. Каталог ищется в `package/<пакет>`, затем
в фидах: на ImmortalWrt HomeProxy лежит в `feeds/luci/applications/`, и
патчи ложатся туда же. Пути в патче считаются от корня репозитория пакета
(`a/htdocs/...`, `a/root/...`). Правила те же, что у патчей фидов: уже есть в
исходниках — пропуск, не подходит — `::warning::` и сборка дальше
(`FEED_PATCH_STRICT=1` — остановка), CRLF снимается. Строки `Requires:` не
поддерживаются: патчи пакета должны быть независимыми.

### HomeProxy (`patches/packages/luci-app-homeproxy/`)

С патчем 004 homeproxy работает и с sing-box 1.13+. Шаг
**Check sing-box compatibility** в `Universal_WRT_Builder.yml` это учитывает:
если генератор сам выбирает форму sniff по версии ядра (признаки патча 004),
для sing-box ≥ 1.13 устаревшие поля в пробный конфиг не добавляются. Без
патча проверка по-прежнему останавливает сборку homeproxy с sing-box 1.13+.
Версия sing-box для homeproxy задаётся в матрице combo (`1.12.x` сейчас) или
полем формы.

Проверено на `immortalwrt/homeproxy` master `edece28` (с ним совпадает
форк YBS-PC/homeproxy) и ветке `dev` `6fb4302`: генератор запускался на конфигах разных режимов и
проверялся настоящим sing-box 1.12.25 (`check` и `run`).

| Файл | Что исправляет |
|---|---|
| `001-urltest-idle-timeout-validation.patch` | LuCI сравнивал интервал URLTest у узла маршрутизации не с его «Idle timeout», а всегда с 1800 с (читал несуществующую опцию `idle_timeout`). Интервал больше 1800 с запрещался даже при большем таймауте, а интервал больше меньшего таймаута пропускался — и sing-box не запускался: `interval must be less or equal than idle_timeout`. |
| `002-dns-default-server.patch` | В `/etc/config/homeproxy` по умолчанию стоял DNS-сервер `local-dns`, которого не существует. В режиме custom, пока настройки DNS не сохранены в LuCI, sing-box не запускался: `default DNS server not found: cfg-local-dns-dns`. Теперь `default-dns`, существующие конфиги мигрируют. Заодно мёртвая опция `dns_strategy` переименована в `default_strategy`, которую читают LuCI и генератор. |
| `003-rpcd-acl-tmp-run.patch` | Права LuCI на логи (`/var/run/homeproxy/*.log`) продублированы для реального пути `/tmp/run/homeproxy/`: rpcd в OpenWrt 25.12 проверяет права по реальному пути (то же, что PR #101 в forkop). |
| `004-sing-box-1.13-sniff-rule.patch` | Поддержка sing-box 1.13+ (в т.ч. 1.14 и extended). 1.13 удалил поля `sniff`/`sniff_override_destination` у входящих подключений, и любой конфиг клиента отвергался. Генератор спрашивает версию у `/usr/bin/sing-box`: ниже 1.13 конфиг прежний (байт в байт), с 1.13 или при неизвестной версии — правило маршрута `action: sniff` сразу после перехвата DNS. «Override destination» на 1.13+ не действует (в новой схеме такой возможности нет). Проверено на 1.12.25, 1.13.21, 1.14.2 (`check` + `run`, режимы custom, основной узел, URLTest; маршрутизация по найденному sniff домену). На 1.14 остаются только предупреждения об устаревании (удаление — в 1.16): `independent_cache`, `store_rdrc`, адресные фильтры и `strategy` в DNS-правилах режима «bypass mainland China». |
| `005-dev-typo-fixes.patch` | Три коммита из ветки `dev` immortalwrt/homeproxy (автор сохранён): ссылки подписок `socks5://` не распознавались (`socsk5` в коде), опечатки в LuCI. Когда `dev` попадёт в `master`, патч опознается как «уже есть». |

## Как сделать новый патч

К дереву OpenWrt:

```sh
git clone --branch openwrt-25.12 https://github.com/openwrt/openwrt.git && cd openwrt
# правки…
git diff > ../patches/002-описание.patch
```

К фиду (пример для forkop):

```sh
git clone https://github.com/ushan0v/forkop.git && cd forkop
# правки…
git diff > ../patches/feeds/forkop/025-описание.patch
```

Проверка на свежем клоне фида, без правок:

```sh
patch -p1 -F 0 --dry-run -d forkop-clean < patches/feeds/forkop/025-описание.patch
```

Файлы применяются по алфавиту, поэтому начинайте имя с номера (`001-`, `002-`…).

